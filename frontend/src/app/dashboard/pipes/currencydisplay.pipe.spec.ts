import { CurrencydisplayPipe } from './currencydisplay.pipe';

describe('CurrencydisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencydisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
